# Task: implement Phase 1 of docs/SPEC_TRUSTWORTHY_FULFILMENT.md

You are the implementer. The spec converged at GO-WITH-CONDITIONS after nine
adversarial review rounds (verdict and twelve implementation-gated conditions:
docs/reviews/SPEC_TRUSTWORTHY_FULFILMENT_CODEX_R9.md). The spec is normative;
where the spec and current code disagree, the spec wins. Do not renegotiate
settled decisions.

## Read first, in order
1. docs/SPEC_TRUSTWORTHY_FULFILMENT.md — whole document. Rollout section
   defines Phase 1 scope; do not pull in later-phase work.
2. docs/reviews/SPEC_TRUSTWORTHY_FULFILMENT_CODEX_R9.md — honor every
   condition that touches Phase 1 work.
3. docs/HANDOFF_CUSTOM_PLAN_FULFILMENT.md and
   docs/MONIKA_RENK_PIPELINE_FINDINGS.md — context for why each control exists.

## Scope: Phase 1 only — "blocked means blocked"
Per the spec's Rollout section:
- S1 (order identity: order_id, schema v2, fail-closed v1 migration)
- S2 (transitional content seal over eagerly built artifacts)
- S4, S6
- B1–B4, including disabling the pre-approval Endure push (D4)
- Typed download tokens
- Post-render validator over the transitional inputs (PlanIR + tp_manifest),
  versioned input as specified
- All new blockers defined in the spec
- A2 (including the source hardcode fix)
- F2, F3 (this-repo part only), F6, F7
- The athlete-m replay fixture with the Phase 1 golden expectation set
  (fixtures/expected values are literal in the spec — implement exactly)

**Gate: the athlete-m Phase 1 golden set passes.** Also run the full existing
test suite; nothing may regress.

## Constraints
- Base: this worktree is branch build/trustworthy-fulfilment at origin/main
  ffd1534. The spec's code claims were verified against an earlier main
  (af284c2); main has moved (PR #92, store-mode). Before editing any file the
  spec cites, re-verify the cited behavior still holds at ffd1534 and adapt
  mechanically if lines moved. If a spec claim is materially invalidated by
  the drift, stop work on that item and record it in
  docs/reviews/PHASE1_IMPLEMENTATION_NOTES.md rather than improvising.
- Skip Phase 0 remote-branch deletion (plan-truth-fixes, plan-ir-v0) — the
  human will handle it. Everything else in Phase 0 is already satisfied
  (clean main-based checkout).
- Commit in small, coherent increments with clear messages, one workstream
  concern per commit where practical. Do NOT push.
- No fabricated data anywhere; the athlete-m fixture must be the de-identified
  synthetic contract from the spec, not real customer data.
- Python 3, pytest. Match existing repo conventions.
- Keep a running log of decisions, deviations, and anything deferred in
  docs/reviews/PHASE1_IMPLEMENTATION_NOTES.md and commit it at the end.

## Definition of done
- All Phase 1 items above implemented, or explicitly logged as blocked with
  the reason in PHASE1_IMPLEMENTATION_NOTES.md.
- athlete-m Phase 1 golden passes; full pytest suite green.
- Working tree clean (everything committed), branch not pushed.
Finish with a summary: commits made, gate status, test results, deviations.
