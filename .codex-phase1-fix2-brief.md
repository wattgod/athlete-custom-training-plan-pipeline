# Task: close the residual findings from Phase 1 review round 2

You are the implementer. Round 2 review
(`docs/reviews/PHASE1_IMPLEMENTATION_CODEX_R2.md`) confirmed r1 blockers
3, 4, 7, 8 FIXED and found blockers 1, 2, 5, 6, 9 PARTIAL plus three new
blockers N1–N3. Fix every PARTIAL residual and all three new blockers. The
review is normative unless factually wrong about the code (rebut with
evidence in the notes file, don't silently ignore).
Spec: `docs/SPEC_TRUSTWORTHY_FULFILMENT.md` wins all conflicts.

## Required fixes

**B1 residual + N3 — bind the apply job and enforce platform.**
- `apply_job.json` must embed order_id, generation_revision, model_seal,
  release_manifest digest, and tp_manifest sha; the driver
  (`tools/tp_apply_driver.js`) must refuse a job missing them and must
  re-check the server gate (order/revision/seal/platform, status APPROVED,
  not legacy) immediately before its first write — a job emitted earlier
  must die after regeneration, seal mismatch, or reassignment. If giving the
  JS driver a live gate check is impractical, the Phase 1-conformant
  alternative is: the driver refuses to run at all unless invoked through
  `tp_apply_order.py` in the same process flow with a short-lived
  gate-check token (expiry minutes, embedded order/revision/seal), and the
  runbook stops advertising standalone driver invocation.
- `check_approval_gate` must require `delivery_platform == "trainingpeaks"`;
  `transition(..., APPLIED)` must verify caller platform equals immutable
  state platform. Regression: platform-mismatch and
  revocation-after-emission tests that exercise the production gate.

**B2 residual — seal verification at every consumer.**
- `transition(..., APPLIED)` verifies the release manifest like APPROVED
  does.
- `/api/confirm` must verify the sealed bytes of everything it is about to
  send (guide, attachments) against the release manifest before sending,
  and materialize non-waivable `SEAL_MISMATCH` into state on failure
  instead of sending. Regression: mutation-after-approval attack through
  the real confirm route.

**B5 residual — complete manifest semantic validation.**
- Validate every executable top-level `tp_manifest.json` projection field
  against PlanIR: `plan_title`, `athlete`, `race.name`, `race.date`,
  `race.priority` (align with the projection owner
  `athletes/scripts/plan_ir.py` — validate whatever it emits, not a
  subset). Regression: mutate each field with unchanged session count and
  assert a blocker fires.

**B6 residual — facts-omitted must survive the guide builder.**
- `training_guide_builder.py` must honor `course_facts_omitted` /
  `course_facts_mode`: when set, it must not re-resolve the race database
  by name and must not flatten database facts (distance, gain, location,
  terrain, climate, metadata) into `race_data`; it renders from
  athlete-supplied facts only.
- Intake must not retain database `location`/`discipline` on an omitted
  profile; if discipline is needed for workout selection, derive from the
  athlete's stated discipline/category input, else omit and let the
  generic path handle it.
- Regression: facts-omitted regeneration through production generation,
  asserting no matched-record fact appears in the rendered guide.

**B9 residual — full-fidelity calendar golden.**
- Check in the complete generated plan-dates output (all fields, day
  descriptors, flags, prefixes) as the golden and compare the whole
  structure, not a hand-selected projection. Regenerate the golden from
  the fixed-clock production run; do not hand-edit it.

**N1 — missing intake must quarantine durably.**
- A paid order with no intake must create order-scoped state with the
  non-waivable `STATE_UNAVAILABLE` (or per-spec synthetic) blocker — a
  durable BLOCKED_REVIEW order awaiting the questionnaire — not a dead
  `pipeline_failed` job requiring manual archaeology. Fix the Stripe
  regression that describes quarantine but asserts none.

**N2 — seal `plan_ir.json`.**
- Add `plan_ir.json` to the sealed private deliverables so the release
  manifest binds both named validator inputs. Athlete-m must assert both
  appear in the release manifest.

## Also
- R2 non-blocking finding 2 (stale Endure branches `_attempt_endure_delivery`
  and confirm-time invitation code): delete the dead code now — it is the
  kind of reachable-later surface r1 blocker 1 was about. Record other
  finding dispositions in the notes.
- Update docs/reviews/PHASE1_IMPLEMENTATION_NOTES.md (per-finding
  disposition + any rebuttals), commit last.

## Constraints
- Phase 1 scope only; hard-fail is an acceptable close for pre-worker
  surfaces, building Phase 4/5 machinery is not.
- Every fix gets a regression test exercising the production path.
- Suite green in your sandbox; the human runs socket tests outside.
- Commit in coherent increments. Do NOT push.

## Definition of done
All eight items fixed with production-path regressions, notes updated,
tree clean, unpushed. End with per-item disposition, commits, test counts.
