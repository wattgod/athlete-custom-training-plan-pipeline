# Task: close the final two Phase 2 blockers from review round 2

You are the implementer. Round 2
(`docs/reviews/PHASE2_IMPLEMENTATION_CODEX_R2.md`) confirmed r1 blockers 1
and 2 resolved and left exactly two items. Close both. The review is
normative unless factually wrong (rebut with evidence in the notes). Spec
wins conflicts. Do not weaken anything that passed.

## Fix 1 — remove the query-token GET path (r1 blocker 3 residual)

The retained GET compatibility path accepts a valid bearer spelling
(URL-encoded variant) that the log-redaction filter does not recognize.
The reviewer's endorsed closure: remove query-token authentication
entirely — the session-authorized POST path is the only way to fetch the
review bundle. Delete the GET compatibility branch rather than patching
the redaction regex; keep the redaction filter as defense-in-depth (and
make it tolerant of percent-encoded spellings anyway).
Regressions: encoded-query bearer on GET → 401/403 and never authenticates;
redaction test with percent-encoded token spelling.

## Fix 2 — seal-mismatch supersession must preserve approval evidence

The new mismatch strategy discards the top-level approval snapshot/waiver
when superseding a revision. Per I5, state must preserve the historical
value-bearing approval record — without authority. Keep the superseded
approval (snapshot, waiver, credential, digest) in an explicit
non-authoritative location (e.g. history entry or `superseded_approvals[]`
with the reason and timestamp) while the new revision carries no approval.
`approval_matches_release` must obviously remain false for it.
Regressions: seal-mismatch supersession retains the full prior approval
evidence as non-authoritative; the page renders it only as history, never
as a valid decision.

## Also
- Rerun the reviewer's named regressions: encoded-query, seal-mismatch
  provenance, focused Phase 1/2 suites.
- Update docs/reviews/PHASE2_IMPLEMENTATION_NOTES.md; stage/commit last.

## Constraints
- Phase 2 scope only. Production-path regressions for both fixes.
- Full suite green in your sandbox; socket tests run outside by the human.
- Commit in coherent increments; do NOT push. If the sandbox blocks git,
  leave the tree dirty and list intended boundaries.

## Definition of done
Both items closed with regressions, notes updated. End with dispositions,
commits/boundaries, test counts.
