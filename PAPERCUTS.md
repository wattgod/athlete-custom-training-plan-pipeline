# Papercuts

Small frictions hit while working. Not bugs, not accomplishments — where this repo needs sanding down.

- 2026-08-23 [fable] sol review via codex exec -> log spammed with nonfatal 'failed to renew cache TTL: missing field base_instructions' errors throughout; review completed fine but the error reads as fatal. Cause: stale codex models_cache schema; a codex upgrade or cache clear likely fixes.
- 2026-08-23 [fable] publishing a second artifact -> live-watch silently failed to arm: session watch limit (5) already consumed; only visible via a lifecycle notification after the fact. Would have missed republish events if that artifact needed watching.
- 2026-08-24 [sonnet-5] Ran intake_to_plan.py for a real coached athlete (steve-wagner) -> after the run finished (deliverables already copied to Downloads at 09:18), block_builder.py/block_chain.py/generate_athlete_package.py showed as modified in git status with timestamps 09:21-09:22 that I never touched. This worktree appears to have a concurrent process/agent editing engine source live during test runs -- a real landmine for anyone trying to reproduce a build or trust 'clean tree' assumptions mid-session. Guess: another Claude Code session sharing this worktree continued workstream-2/3 implementation while my pipeline run was still finishing up.
- 2026-08-24 [sonnet-5] copy_to_downloads()'s final 'Package contents' summary prints 'workouts/ 0 ZWO files' (intake_to_plan.py:4345) right above 'All deliverables present' -- reads like a broken build even though it's by design (ZWO stays sealed in the athlete dir pending order approval, per the comment at intake_to_plan.py:3767). Fix: either count athlete_dir/workouts instead of the Downloads copy, or say '(sealed pending approval)' instead of a bare zero.
- 2026-08-29 [fable] TP work via playwriter blocked: relay CDP server won't bind (5s timeout) while a second playwriter MCP instance is running — 6 node playwriter procs alive, port 19988 shows nothing in lsof. Reset fails repeatedly. Didn't kill the other instance in case another session is mid-TP-write. Needs a way to detect/steal-or-queue the relay, or a clearer 'relay owned by another session' error.
- 2026-08-29 [sonnet-5] Uploading a ~42KB xlsx to Google Drive via mcp__claude_ai_Google_Drive__create_file -> base64Content must be inline in the tool call, no chunked/resumable upload path exists, and a single turn's generated tool-call content is capped around 24-27K chars (~8192 output tokens) -- so any binary file whose base64 exceeds ~18KB raw / ~24KB base64 cannot be uploaded this way at all, silently truncating mid-string on largr files (broke base64 validity, not an explicit size-limit error). No rclone/gdrive CLI configured for this Drive account either as a fallback. Fix: either the create_file tool needs multipart/resumable upload support, or the harness needs a chunked-content variant; until then, files near/above ~18KB need a human to upload manually or a properly Drive-scoped service account driven from Bash (not attempted here -- would have required granting Drive access to an unrelated GSC service account, judged out of scope to do unilaterally).
- 2026-08-29 [sonnet-5] Ran build_tp_plan_payload.py on an athlete with a pre-existing unresolved AE-3.1 FAIL (unrelated to my AE-9.11 work) -> it silently skips writing plan_payload.json/notes_payload.json (only exclusions.json+lint.json land) and exits 1, with no message pointing at --allow-known-fails as the fix. Had to build the allow-list JSON myself from lint.json's unresolved_fails. A one-line stderr hint ('N unresolved FAILs -- pass --allow-known-fails or fix them') would save the round-trip.
