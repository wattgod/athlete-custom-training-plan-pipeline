# Task: fix all nine blockers from the Phase 1 adversarial review

You are the implementer. Your previous Phase 1 implementation was
adversarially reviewed and returned NO-GO with nine blockers:
`docs/reviews/PHASE1_IMPLEMENTATION_CODEX_R1.md`. Fix every blocker. The
review's findings are normative unless a finding is factually wrong about
the code — in that case, do not change code; rebut it with file/line
evidence in your notes file.

## Read first
1. docs/reviews/PHASE1_IMPLEMENTATION_CODEX_R1.md — the whole report,
   including the conformance matrix, non-blocking findings, and the R9
   condition table.
2. docs/SPEC_TRUSTWORTHY_FULFILMENT.md — the spec clauses each blocker
   cites. The spec wins over both the review and the code if they conflict.

## Required fixes (all nine blockers)

1. **Close every bypass route.** The legacy paths in
   `athletes/scripts/deliver_package.py`, `athletes/scripts/email_delivery.py`,
   `tools/tp_apply_order.py` (remove `--skip-approval-check`; require
   order/revision/seal binding, not just status==APPROVED),
   `delivery/trainingpeaks/adapter.py`, and the no-intake webhook path must
   refuse to release artifacts or apply without a seal-bound APPROVED order.
   Per B1/S2: every consumer verifies. Where a legacy CLI cannot reasonably
   verify, it must hard-fail with a message pointing at the gated path — not
   warn and proceed.
2. **Enforce the seal at approval and after.** `transition(..., APPROVED)`
   must call `verify_release_manifest` against the actual bytes;
   `persist_deliverables` must never delete/rebuild an already-sealed
   same-revision directory (require a new revision via `write_generation`);
   fix the verify-then-reopen TOCTOU on download (verify the same open file
   handle you serve); any seal mismatch must merge a non-waivable
   `SEAL_MISMATCH` blocker into state, not just 409.
3. **Make v1 quarantine authority-free and complete.** `confirm_after_send`
   and `/api/confirm` must reject quarantined legacy orders; the status
   endpoint must not present preserved legacy APPROVED as actionable
   authority (surface `legacy: true` and have consumers refuse); add a
   startup scan that migrates every v1 file (not only lazy on lookup), and
   fix the lookup shadowing where an existing v2 lookup hides an unmigrated
   v1 file.
4. **No false success.** If persistence (including the quarantine fallback)
   fails, the job must not be marked succeeded and no success notice may be
   sent. Fix the regression test that currently asserts the forbidden
   behavior.
5. **Validate the real transitional artifacts.** The post-render validator
   must semantically validate `tp_manifest.json` content (not just session
   count) and must run against the final written bytes — validate after the
   last rewrite, or make the rewrite happen before validation. Fix the
   altitude blocker to read the schema production actually emits (align with
   both `intake_to_plan.py`'s `target_race` fields and
   `training_guide_builder.py`'s `race_metadata` trigger) and add an
   altitude test using a production-shaped snapshot.
6. **COURSE_UNRESOLVED must actually strip course facts.** When the course
   is unresolved, no matched-record course fact (elevation etc.) may reach
   `target_race`/plan/guide; implement the facts-omitted path per S4/C2 so
   regeneration from athlete-supplied facts clears the blocker.
7. **A2 unknown device tokens.** Preserve unknown tokens verbatim (original
   casing/spacing after comma/newline split + trim) and emit them as
   confirmation items; fix the test to assert verbatim preservation and the
   confirmation.
8. **Fail-closed tokens + real revocation.** Missing token key/secret must
   refuse to issue/verify (no `dev-secret` fallback — startup or first-use
   hard failure); add an authenticated operational revocation path callable
   for a real issued jti/kid; add tests for missing-secret refusal and
   legacy-token rejection, without pre-setting keys in every test.
9. **Honest athlete-m gate.** The Phase 1 golden must be produced by
   running the production generation path (date calculator, blocker
   assembly, package/artifact generation) under a fixed clock and the
   spec's literal fixture inputs — not by reading expected files or
   hand-returning blocker lists. The literal `expected/phase1.json` stays;
   the test must derive actuals from production code and compare.

Also address non-blocking finding 3 (the missing negative tests) — most of
them are the regression tests the blockers require anyway. Non-blocking
findings 1–2 (test skips, stale Endure branches): tighten where cheap;
otherwise record in notes. Findings 4–6: record disposition in notes; fix 5
(reject later-phase statuses in schema validation for now) since it is a
one-line tightening.

## Constraints
- Phase 1 scope only. Do not implement later-phase functionality to "fix"
  a blocker (e.g. blocker 1 is satisfied by hard-failing legacy paths, not
  by building the Phase 5 worker).
- Every blocker fix needs a regression test exercising the production path.
- Full existing suite must stay green in your sandbox; socket-based tests
  will be run by the human outside the sandbox.
- Commit in coherent increments (roughly one blocker or cluster per
  commit). Do NOT push.
- Update docs/reviews/PHASE1_IMPLEMENTATION_NOTES.md with per-blocker
  disposition and any rebuttals, and commit it last.

## Definition of done
All nine blockers fixed with regression tests, suite green (minus
socket-skips), notes updated, tree clean, unpushed. End with a summary:
per-blocker disposition, commits, test counts.
