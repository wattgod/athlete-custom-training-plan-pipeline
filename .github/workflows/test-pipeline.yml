name: Test Pipeline

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'athletes/scripts/**'
      - 'athletes/config/**'
      - 'athletes/*/profile.yaml'
      - 'webhook/**'
      - 'delivery/**'
      - 'tools/**'
      - 'scripts/**'
      - 'tests/fixtures/**'
      - 'config.yaml'
      - 'requirements.txt'
      - '.github/workflows/**'
  pull_request:
    branches: [main]
    paths:
      - 'athletes/scripts/**'
      - 'athletes/config/**'
      - 'webhook/**'
      - 'delivery/**'
      - 'tools/**'
      - 'scripts/**'
      - 'tests/fixtures/**'
      - 'config.yaml'
      - 'requirements.txt'
      - '.github/workflows/**'
  # Run regularly even without commits — catches drift from external data
  # (race dates rolling past, library or config edits) and proves the whole
  # order path still produces send-worthy plans.
  schedule:
    - cron: '0 14 * * 1'  # Mondays 14:00 UTC

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Chromium (PDF engine — must match production)
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser || sudo apt-get install -y chromium
          which chromium-browser || which chromium || true

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml python-dateutil pytest pypdf pillow requests beautifulsoup4
          # webhook deps — the acceptance suite drives the real order path
          pip install -r webhook/requirements.txt || echo "webhook reqs partial"

      - name: Lint (syntax + undefined names)
        run: |
          pip install flake8
          flake8 athletes/scripts/*.py --count --select=E9,F63,F7,F82 --show-source --statistics

      - name: Run full unit + guard suite
        working-directory: athletes/scripts
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          # Acceptance tests auto-skip here (no GG_RUN_ACCEPTANCE) — this is
          # the fast layer: unit tests + all the content/PDF/compliance guards.
          python -m pytest . -q --ignore=test_order_acceptance.py

      - name: Run recurring testing-process unit tests
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          python -m pytest \
            tools/test_canary_probe.py \
            tools/test_audit_fulfillment_states.py \
            tools/test_daily_order_drill.py \
            webhook/tests/test_state_audit_endpoint.py \
            scripts/test_check_fixture_freshness.py -q

      - name: Run focused Phase 5 publisher tests
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          python -m pytest \
            delivery/trainingpeaks/test_phase5_service.py \
            delivery/trainingpeaks/test_playwright_transport.py \
            delivery/trainingpeaks/test_worker_service.py \
            webhook/tests/test_fulfillment_state.py -q

      - name: Run Phase 5 browser payload harness
        run: node tools/test_tp_phase5_browser_payload.mjs

      - name: Run ORDER ACCEPTANCE (real pipeline end-to-end + PDF)
        working-directory: athletes/scripts
        env:
          PYTHONPATH: ${{ github.workspace }}
          GG_RUN_ACCEPTANCE: '1'
        run: |
          # The send-worthy contract: each golden order runs the real
          # pipeline and the deliverable must pass every coherence check.
          python -m pytest test_order_acceptance.py -q
