[pytest]
markers =
    acceptance: full end-to-end order acceptance (slow; runs the real pipeline + PDF). Opt-in via GG_RUN_ACCEPTANCE=1.
    library_e2e: C4 library-selection end-to-end run against the real pipeline
filterwarnings =
    ignore::DeprecationWarning
