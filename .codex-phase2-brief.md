# Task: implement Phase 2 of docs/SPEC_TRUSTWORTHY_FULFILMENT.md — the review surface

You are the implementer. Phase 1 ("blocked means blocked") is complete on
this branch and passed adversarial review
(`docs/reviews/PHASE1_IMPLEMENTATION_CODEX_R4.md`, GO). You are building
Phase 2 on top of it. The spec is normative; where spec and code disagree,
the spec wins. Do not renegotiate settled decisions. Do not weaken any
Phase 1 control — every Phase 1 regression test must stay green untouched
unless Phase 2 legitimately supersedes a surface, in which case the test
evolves with cited spec justification in your notes.

## Read first, in order
1. docs/SPEC_TRUSTWORTHY_FULFILMENT.md — whole document; the Rollout
   section defines Phase 2 scope: **S3, C1, C2 surface, C4; approval
   snapshot with values; policy enforcement.**
2. docs/reviews/SPEC_TRUSTWORTHY_FULFILMENT_CODEX_R9.md — honor every
   condition touching Phase 2 work.
3. docs/reviews/PHASE1_IMPLEMENTATION_CODEX_R1.md through _R4.md — know
   what was attacked and why, so you don't reopen a closed hole.
4. docs/reviews/PHASE1_IMPLEMENTATION_NOTES.md — current dispositions.

## Scope: Phase 2 only, per the spec's Rollout section
- S3 as specified in the spec (review surface / approval snapshot
  workstream) — implement exactly what the spec assigns to Phase 2.
- C1, C2 surface, C4 per their spec sections.
- Approval snapshot with values: the approval record must bind the coach's
  decision to the seal AND to the reviewed values themselves, per the
  spec's snapshot requirements.
- Policy enforcement per the spec (non-waivable rules server-side, waiver
  coverage, required confirmations enforced at approval time on the page
  path).
- The Phase 2 gate ("one real order approved entirely on the page with a
  complete, seal-bound approval snapshot") requires a live order and a
  human coach — you cannot execute it. Your obligation: make the page path
  fully functional and tested so the coach can execute the gate, and add a
  deterministic test that walks the entire page approval flow for the
  athlete-m fixture (login-to-approved, snapshot persisted, seal-bound).
- Do NOT implement Phase 3+ work (canonical model, D0 contract, worker,
  apply machinery). Hard boundaries stay hard.

## Constraints
- Match the repo's existing web stack and conventions — inspect how the
  webhook app serves pages/endpoints today before choosing any approach.
  No new frameworks or heavy dependencies without clear necessity.
- The review surface must be authenticated (consistent with existing
  operator auth patterns in the app) and must never leak artifacts to
  unauthenticated callers; downloads on the page go through the existing
  typed-token path.
- XSS discipline: all order/athlete-derived values rendered with proper
  escaping; no innerHTML with data-derived values; JSON embedded in pages
  must be script-safe.
- Every new control gets a production-path regression test, including
  negative tests (approve with missing confirmation, waiver not covering
  all blockers, non-waivable in waiver, stale revision, seal mismatch at
  page-approval time).
- Full suite green in your sandbox; socket tests run outside by the human.
- Commit in coherent increments. Do NOT push. If git commit fails due to
  sandbox restrictions, leave the tree uncommitted and list your intended
  commit boundaries in the final summary.
- Keep docs/reviews/PHASE2_IMPLEMENTATION_NOTES.md with decisions,
  deviations, rebuttals; commit (or stage) it last.

## Definition of done
Phase 2 scope implemented or explicitly logged as blocked with reasons;
athlete-m page-approval flow test passes; full suite green (minus socket
skips); notes written. End with: per-item disposition, commits (or
intended boundaries), test counts, and anything the coach must do to
execute the live gate.
