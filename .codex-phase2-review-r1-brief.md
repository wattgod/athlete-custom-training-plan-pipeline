# Task: adversarial review of the Phase 2 implementation — round 1

You are the adversarial reviewer. Phase 1 converged at GO after four rounds
(`docs/reviews/PHASE1_IMPLEMENTATION_CODEX_R1.md`–`_R4.md`). Phase 2 (the
review surface) was implemented in commits `73a96ae`, `65df723`, `88b8558`,
`43aacb3` on this branch, stacked on Phase 1. Notes claim full scope:
`docs/reviews/PHASE2_IMPLEMENTATION_NOTES.md` — audit, don't trust. You did
not write this code. Assume it is wrong until proven otherwise.

Normative: `docs/SPEC_TRUSTWORTHY_FULFILMENT.md` (Rollout defines Phase 2:
S3, C1, C2 surface, C4; approval snapshot with values; policy enforcement).
R9 conditions: `docs/reviews/SPEC_TRUSTWORTHY_FULFILMENT_CODEX_R9.md`.

## What to attack

1. **Spec conformance item by item** (S3, C1, C2 surface, C4, snapshot,
   policy): cite implementing code or flag missing/partial. The approval
   snapshot must bind coach decision to seal AND reviewed values; verify
   the snapshot actually contains the values shown, not references that
   can drift.
2. **Approval authority.** Can approval happen with: missing/partial
   confirmation set, waiver not covering every blocker, a non-waivable id
   inside a waiver, stale generation revision, seal mismatch at
   page-approval time, a revoked/expired review token, a session from a
   different order, CSRF? Attack the endpoint directly, not just the page.
3. **The page as a leak.** Unauthenticated or expired-session access to
   order data; artifacts reachable without the typed-token path; athlete
   PII in the login shell, error pages, logs, or cache-able responses;
   XSS via athlete-derived values (names, race names, device strings,
   blocker messages); script-embedded JSON injection (`</script>`).
4. **Token/session discipline.** Review-token scope (order, athlete,
   revision, action), expiry, revocation propagation to live sessions,
   fragment-exchange downgrade, session fixation, secrets in URLs that
   land in logs.
5. **Phase 1 erosion.** Any Phase 1 control weakened, any Phase 1
   regression test modified without justified supersession, any closed
   bypass reopened through the new page path (the review page is a new
   consumer — does it verify seal before rendering/approving?).
6. **Phase 3+ leakage** and renegotiated settled decisions.
7. **Test honesty.** New tests exercising production paths vs mocking the
   property under test; missing negative cases.

## Constraints
- Read-only; write only your report. Do not commit.
- Sandbox cannot run socket tests; human-run full suite outside:
  2380 passed, 86 skipped, 0 failed.
- Judge Phase 2 against Phase 2 scope; the live gate (one real order
  approved on-page) is explicitly a human step — verify the path is
  complete and enforced, not that it was executed.

## Output
`docs/reviews/PHASE2_IMPLEMENTATION_CODEX_R1.md`: verdict (GO /
GO-WITH-CONDITIONS / NO-GO), conformance matrix, numbered blockers with
evidence + spec clause, non-blocking findings, unverifiable items,
one-paragraph summary.
