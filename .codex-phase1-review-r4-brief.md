# Task: adversarial review of the Phase 1 implementation — round 4

You are the adversarial reviewer. History: r1 NO-GO (nine blockers), r2
NO-GO (five partial + N1–N3), r3 NO-GO with only two open items — the B1
driver residual and N4 Endure false-advance
(`docs/reviews/PHASE1_IMPLEMENTATION_CODEX_R1.md`, `_R2.md`, `_R3.md`).
The implementer claims both are closed in commits `4301d5b`, `102bd89`,
`67b54c7` using the closures r3 explicitly endorsed: hard-disabling the
transitional TP browser driver as a release path, and refusing Endure
APPLIED/confirm while Endure is disabled. Audit the notes
(`docs/reviews/PHASE1_IMPLEMENTATION_NOTES.md`), don't trust them.

## Method
1. Verify both closures in code + production-path regressions:
   - B1: can any invocation of `tools/tp_apply_order.py` or
     `tools/tp_apply_driver.js` (or any other entry point) still emit or
     execute an apply job? Is the retained gate/token infrastructure
     write-incapable? Does any doc still advertise the driver flow?
   - N4: replay the r3 attack exactly (approve clean Endure order, POST
     `{"to":"APPLIED","platform":"endure","evidence":"x"}`, then confirm).
     Check the manual/attested path allowed in Phase 1 is not itself a
     platform-confusion hole (e.g. Endure order attested as "manual" then
     confirmed with TP copy).
2. Re-attack the changed surface and check the r3 non-blocking findings'
   dispositions (guarded guide helper, token hardening, seal coverage).
3. Verify no regression of anything previously FIXED across r1–r3.
4. Final judgment on the Phase 1 gate: is "blocked means blocked" now an
   end-to-end invariant for every reachable Phase 1 surface? Judge against
   Phase 1 scope only — hard-fail closures are conformant for pre-worker
   surfaces; do not demand Phase 2–5 machinery.

Normative: `docs/SPEC_TRUSTWORTHY_FULFILMENT.md`; r9 conditions in
`docs/reviews/SPEC_TRUSTWORTHY_FULFILMENT_CODEX_R9.md`.

## Constraints
- Read-only; write only your report. Do not commit.
- Sandbox cannot run socket tests; the human ran the full suite outside:
  2357 passed, 86 skipped, 0 failed.

## Output
`docs/reviews/PHASE1_IMPLEMENTATION_CODEX_R4.md`: verdict (GO /
GO-WITH-CONDITIONS / NO-GO), disposition of the two r3 items, any new
blockers with evidence + spec clause, non-blocking findings, unverifiable
items, one-paragraph summary. If GO or GO-WITH-CONDITIONS, list exactly
what conditions remain and which later phase owns each.
