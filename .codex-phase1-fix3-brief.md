# Task: close the final two Phase 1 blockers from review round 3

You are the implementer. Round 3
(`docs/reviews/PHASE1_IMPLEMENTATION_CODEX_R3.md`) confirmed every carried
item FIXED except one residual and found one new blocker. Close both using
the reviewer's explicitly-endorsed Phase 1-conformant closures. Spec
(`docs/SPEC_TRUSTWORTHY_FULFILMENT.md`) wins conflicts.

## Fix 1 — B1: hard-disable the live TP browser driver as a release path

The reviewer's endorsed closure (R3 §1, last paragraph): Phase 1 does not
need lease machinery — disable the driver like the other pre-worker
surfaces.

- `tools/tp_apply_order.py` must refuse to emit an executable
  `apply_job.json` / driver runbook. Replace with a hard failure that
  explains: automated apply returns in Phase 4/5 via the worker; until
  then the coach applies manually in the TrainingPeaks UI and records
  APPLIED through the authenticated transition with evidence.
  Keep the gate-check/token infrastructure code (Phase 4/5 will want it)
  but nothing may emit an executable job.
- `tools/tp_apply_driver.js` must refuse to run (top-of-file hard exit
  with the same message), so a stale previously-emitted job cannot be
  executed either.
- Update any runbook/docs text that advertises the driver flow.
- Regression: production-path tests that the CLI refuses job emission and
  that the gate endpoints still function for future phases (status/token
  behavior unchanged).

## Fix 2 — N4: Endure stays fully disabled

- `transition(..., APPLIED)` must reject `platform == "endure"` (and any
  platform other than "trainingpeaks" and the manual/attested path the
  spec allows in Phase 1) while Endure is disabled — cite D4/R9 condition
  11 in the error.
- `/api/confirm` must check `delivery_platform`: refuse confirmation for
  Endure orders, and never send TrainingPeaks-specific copy for a
  non-TrainingPeaks order.
- Regression: the exact attack in R3 — approve a clean Endure order,
  attempt `{"to":"APPLIED","platform":"endure","evidence":"x"}` via the
  authenticated route, assert refusal; attempt confirm, assert refusal.

## Also
- Check R3 "Re-attack results and non-blocking findings" (§124 onward) and
  fix anything cheap (e.g. the residual facts-rehydrating helper if it is
  genuinely off the production path, either delete it or guard it);
  record dispositions for the rest in
  docs/reviews/PHASE1_IMPLEMENTATION_NOTES.md.
- Update the notes file with both blocker dispositions; commit it last.

## Constraints
- Phase 1 scope only. Hard-fail closures, no new machinery.
- Every fix gets a production-path regression test.
- Suite green in your sandbox (socket tests run outside by the human).
- Commit in coherent increments. Do NOT push.

## Definition of done
Both blockers closed with regressions, cheap re-attack findings fixed or
dispositioned, notes updated, tree clean, unpushed. End with dispositions,
commits, test counts.
