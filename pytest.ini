[pytest]
addopts = --import-mode=importlib
markers =
    acceptance: full end-to-end order acceptance (opt-in with GG_RUN_ACCEPTANCE=1)
    library_e2e: C4 library-selection end-to-end run against the real pipeline
